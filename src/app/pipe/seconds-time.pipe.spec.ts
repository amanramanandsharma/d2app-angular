import { SecondsTimePipe } from './seconds-time.pipe';

describe('SecondsTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsTimePipe();
    expect(pipe).toBeTruthy();
  });
});
